# Welcome

This folder is the default workspace for local-agent-chat.

The assistant can list these files and read them. It cannot see the rest of
the disk unless you point `WORKSPACE_ROOT` somewhere else.

Try asking:

- What files are in this workspace?
- Summarize this file in three bullets.
